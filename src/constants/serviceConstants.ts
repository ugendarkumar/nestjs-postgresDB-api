export enum serviceUri {
  geoJson = '/geojson',
}

export enum serviceMethods {
  Post = 'post',
  Get = 'get',
  Put = 'put',
  Patch = 'patch',
}

export enum contentType {
  applicationJson = 'application/json',
  multipart = 'multipart/form-data',
}
